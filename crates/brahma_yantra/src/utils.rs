use crate::*;

// pub fn initialise_lane_state(
//     mut state: LaneState,
//     transitions: &[Entity],
// ) -> LaneState {
//     state.transitions = transitions.iter().cloned().collect();
//     return state;
// }
